import 'package:flutter/material.dart';

class TopSeller extends StatelessWidget {
  const TopSeller({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text("Top seller"),),
    );
  }
}