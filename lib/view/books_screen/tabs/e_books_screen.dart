import 'package:flutter/material.dart';

class EBooksScreen extends StatelessWidget {
  const EBooksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("E Books"),),
    );
  }
}