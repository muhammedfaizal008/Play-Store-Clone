import 'package:flutter/material.dart';

class PremiumScreenApps extends StatelessWidget {
  const PremiumScreenApps({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Premium Content")),
    );
  }
}