import 'package:flutter/material.dart';

class ChildrenScreenApps extends StatelessWidget {
  const ChildrenScreenApps({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Children Content")),
    );
  }
}