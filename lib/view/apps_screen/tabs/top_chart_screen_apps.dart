import 'package:flutter/material.dart';

class TopChartScreenApps extends StatelessWidget {
  const TopChartScreenApps({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Top Charts Content")),
    );
  }
}